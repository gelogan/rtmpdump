---
name: doxygen
description: 生成专业级 C/C++ Doxygen 注释
invokable: true
---
请仔细阅读我选中的这段完整代码逻辑。然后，只给我输出这个函数头部的 Doxygen 中文注释。

【内容要求】：
1. 准确解释所有参数的业务作用。如果函数无参数，请跳过此项。
2. 必须使用 Doxygen 的原生方向标记！严格根据代码中的指针读写逻辑和 const 属性，使用 @param[in]、@param[out] 或 @param[in,out]。
3. 必须说明函数的返回值意义。如果是状态码（如 0, -1, true, false)，请明确列举并使用 @retval 标签说明每种状态代表的具体情况。

【格式要求】：
1. 必须使用标准的 C/C++ 多行注释格式（首行 /**，末行 */）。
2. 中间的每一行都必须以星号 * 开头并保持对齐。
3. 为了排版美观，请将参数名和解释文字做空格对齐。
4. 请务必将生成的注释包裹在一个 ```c 的代码块中输出，绝不要输出任何原有代码和多余的废话！

代码如下：
{{{input}}}